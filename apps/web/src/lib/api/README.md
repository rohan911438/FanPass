Typed fetch client for `apps/api`, one file per resource (tickets, listings, wallet, trust). UI components
call these, never `fetch` directly. Built in Phase 2 onward — see `docs/ARCHITECTURE.md` §1.
