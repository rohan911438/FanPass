PORT=4000
NODE_ENV=development
WEB_ORIGIN=http://localhost:3000

# Firebase Admin SDK — from a service account JSON, not the client config
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_STORAGE_BUCKET=
