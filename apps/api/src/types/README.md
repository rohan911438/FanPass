# types

API-only types that don't belong in `@fanpass/shared` (request/response DTOs, internal service contracts).
Anything used by both `apps/web` and `apps/api` belongs in `packages/shared` instead.
