# repositories

The ONLY layer allowed to import `config/firebaseAdmin`. One file per Firestore collection
(`userRepository`, `ticketRepository`, `listingRepository`, `trustScoreRepository`, …) matching
`docs/ARCHITECTURE.md` §4. Filled in from Phase 2 onward.
