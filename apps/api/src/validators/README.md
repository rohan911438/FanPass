# validators

Request validation middleware. Wraps Zod schemas from `@fanpass/shared` (and any API-only schemas) as
Express middleware — `validate(schema)` used in route definitions. Filled in from Phase 2 onward.
