{
  "name": "fanpass",
  "private": true,
  "version": "0.1.0",
  "description": "FanPass — AI-powered Ticket Trust Network for peer-to-peer World Cup ticket resale.",
  "workspaces": [
    "apps/web",
    "apps/api",
    "apps/contracts",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api",
    "build:shared": "npm run build --workspace=packages/shared",
    "lint:web": "npm run lint --workspace=apps/web",
    "contracts:compile": "npm run compile --workspace=apps/contracts",
    "contracts:test": "npm run test --workspace=apps/contracts",
    "contracts:deploy:testnet": "npm run deploy:testnet --workspace=apps/contracts"
  },
  "engines": {
    "node": ">=20"
  }
}
