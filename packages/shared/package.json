{
  "name": "@fanpass/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types and Zod schemas shared between apps/web and apps/api — single source of truth, enforced on both ends.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "viem": "^2.55.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
